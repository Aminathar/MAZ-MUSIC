# ─── Core (always required) ──────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.0
python-multipart>=0.0.9
psutil>=5.9

# ─── Audio analysis & I/O ────────────────────────────────────
numpy>=1.24
soundfile>=0.12
librosa>=0.10

# ─── Voice pipeline / stem separation (optional) ─────────────
# Required only if you use voice conversion or the Stems Export feature.
audio-separator>=0.21
demucs>=4.0
torch>=2.0
torchaudio>=2.0
